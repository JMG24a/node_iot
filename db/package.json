{
  "name": "iot-db",
  "version": "1.0.0",
  "description": "db para nuestra iot",
  "main": "index.js",
  "scripts": {
    "migration:delete": "sequelize-cli db:migration:undo:all",
    "migration:revert": "sequelize-cli db:migration:undo",
    "migration:generate": "sequelize-cli migration:generate --name",
    "migration:run": "sequelize-cli db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jose <jmg24a@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.18.0",
    "sequelize-cli": "^6.4.1"
  },
  "keywords": []
}
